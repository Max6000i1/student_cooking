<%= form_with(model: recette, multipart: true) do |form| %>
  <% if recette.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recette.errors.count, "error") %> prohibited this recette from being saved:</h2>

      <ul>
        <% recette.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.label :temps, style: "display: block" %>
    <%= form.number_field :temps %>
  </div>

  <div>
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
