<%= form_with(model: etape) do |form| %>
  <% if etape.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(etape.errors.count, "error") %> prohibited this etape from being saved:</h2>

      <ul>
        <% etape.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.label :recette_id, style: "display: block" %>
    <%= form.text_field :recette_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
